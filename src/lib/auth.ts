import crypto from "node:crypto";import fs from "node:fs";import path from "node:path";
export type StudentUser={id:string;username:string;candidateNo:string;name:string;grade:number;school?:string;pinHash:string;createdAt:number;active:boolean;role:"student"|"admin"};export type PublicStudent=Omit<StudentUser,"pinHash">;const DIR=path.join(process.cwd(),"private","users"),USERS=path.join(DIR,"users.json"),SECRET=path.join(DIR,"session-secret.txt");export const SESSION_COOKIE="kangaroo_session";function ensure(){fs.mkdirSync(DIR,{recursive:true})}function secret(){ensure();if(!fs.existsSync(SECRET))fs.writeFileSync(SECRET,crypto.randomBytes(48).toString("hex"),{mode:0o600});return fs.readFileSync(SECRET,"utf8").trim()}export function hashPin(pin:string){const salt=crypto.randomBytes(16).toString("hex");return `scrypt$${salt}$${crypto.scryptSync(pin,salt,32).toString("hex")}`}function verifyPin(pin:string,encoded:string){const[k,salt,expected]=encoded.split("$");if(k!=="scrypt"||!salt||!expected)return false;const a=crypto.scryptSync(pin,salt,32),b=Buffer.from(expected,"hex");return a.length===b.length&&crypto.timingSafeEqual(a,b)}export function loadUsers():StudentUser[]{ensure();if(!fs.existsSync(USERS))return[];try{const x=JSON.parse(fs.readFileSync(USERS,"utf8"));return Array.isArray(x)?x.map(u=>({...u,role:u.role==="admin"?"admin":"student"})):[]}catch{return[]}}export function saveUsers(x:StudentUser[]){ensure();fs.writeFileSync(USERS,JSON.stringify(x,null,2))}function pub(u:StudentUser):PublicStudent{const{pinHash,...x}=u;void pinHash;return x}export function authenticate(k0:string,pin:string){const k=k0.trim().toLowerCase(),u=loadUsers().find(x=>x.active&&(x.username.toLowerCase()===k||x.candidateNo.toLowerCase()===k));return u&&verifyPin(pin,u.pinHash)?pub(u):null}export function publicUserById(id:string){const u=loadUsers().find(x=>x.id===id&&x.active);return u?pub(u):null}export function createSessionToken(uid:string,ttl=604800){const p=Buffer.from(JSON.stringify({uid,exp:Math.floor(Date.now()/1000)+ttl})).toString("base64url"),s=crypto.createHmac("sha256",secret()).update(p).digest("base64url");return `${p}.${s}`}export function userFromSessionToken(t:string|undefined|null):PublicStudent|null{if(!t)return null;const[p,s]=t.split(".");if(!p||!s)return null;const e=crypto.createHmac("sha256",secret()).update(p).digest("base64url"),a=Buffer.from(s),b=Buffer.from(e);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))return null;try{const x=JSON.parse(Buffer.from(p,"base64url").toString()) as {uid?:string;exp?:number};return x.uid&&x.exp&&x.exp>=Math.floor(Date.now()/1000)?publicUserById(x.uid):null}catch{return null}}export const isAdmin=(u:PublicStudent|null|undefined)=>u?.role==="admin";
export function createStudent(input:{username:string;candidateNo:string;name:string;grade:number;school?:string;pin:string}){const users=loadUsers(),username=input.username.trim(),candidateNo=input.candidateNo.trim(),name=input.name.trim();if(!username||!candidateNo||!name||input.pin.length<4)throw new Error("学生信息或 PIN 不完整");if(users.some(x=>x.username.toLowerCase()===username.toLowerCase()))throw new Error("用户名已存在");if(users.some(x=>x.candidateNo.toLowerCase()===candidateNo.toLowerCase()))throw new Error("准考证号已存在");const u:StudentUser={id:`stu_${crypto.randomBytes(8).toString("hex")}`,username,candidateNo,name,grade:input.grade,school:input.school?.trim()||undefined,pinHash:hashPin(input.pin),createdAt:Date.now(),active:true,role:"student"};users.push(u);saveUsers(users);return pub(u)}
