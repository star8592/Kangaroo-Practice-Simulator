import{NextRequest,NextResponse}from"next/server";import{authenticate,createSessionToken,SESSION_COOKIE}from"@/lib/auth";export async function POST(req:NextRequest){try{const b=await req.json(),u=authenticate(String(b?.username||b?.candidateNo||""),String(b?.pin||""));if(!u)return NextResponse.json({error:"账号、准考证号或 PIN 不正确"},{status:401});const r=NextResponse.json({ok:true,user:u});r.cookies.set(SESSION_COOKIE,createSessionToken(u.id),{httpOnly:true,sameSite:"lax",secure:req.nextUrl.protocol==="https:",path:"/",maxAge:604800});return r}catch{return NextResponse.json({error:"登录失败"},{status:400})}}
