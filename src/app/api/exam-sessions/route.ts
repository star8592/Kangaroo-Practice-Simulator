import{NextRequest,NextResponse}from"next/server";import{SESSION_COOKIE,userFromSessionToken}from"@/lib/auth";import{isExamBundleStudentReady,loadExamBundle}from"@/lib/question-bank";import{activeExamSession,startExamSession}from"@/lib/exam-session";const user=(r:NextRequest)=>userFromSessionToken(r.cookies.get(SESSION_COOKIE)?.value);export async function GET(r:NextRequest){const u=user(r);if(!u)return NextResponse.json({error:"请先登录考生账号"},{status:401});const id=r.nextUrl.searchParams.get("examId")||"";return id?NextResponse.json({session:activeExamSession(u.id,id)}):NextResponse.json({error:"missing examId"},{status:400})}export async function POST(r:NextRequest){try{const u=user(r);if(!u)return NextResponse.json({error:"请先登录考生账号"},{status:401});const b=await r.json(),id=String(b?.examId||""),bundle=loadExamBundle(id);if(!isExamBundleStudentReady(bundle))return NextResponse.json({error:"Exam is not student-ready"},{status:404});return NextResponse.json({...startExamSession(u.id,id,bundle.profile.durationSeconds),durationSeconds:bundle.profile.durationSeconds})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"无法创建考试会话"},{status:500})}}
