"use client";import Link from "next/link";import { useEffect,useState } from "react";import { useRouter } from "next/navigation";
type U={name:string;candidateNo:string;role?:"student"|"admin"};
export default function SessionNav(){const router=useRouter(),[user,setUser]=useState<U|null|undefined>(undefined);useEffect(()=>{fetch('/api/auth/me').then(async r=>r.ok?(await r.json()).user:null).then(setUser).catch(()=>setUser(null))},[]);async function logout(){await fetch('/api/auth/logout',{method:'POST'}).catch(()=>{});setUser(null);router.push('/login');router.refresh()}if(user===undefined)return <span className="nav-session">…</span>;if(!user)return <Link href="/login">考生登录</Link>;return <>{user.role==='admin'&&<Link href="/admin/questions">题库审核</Link>}<Link href="/student">学习报告</Link><span className="nav-candidate">{user.name}</span><button className="nav-logout" onClick={logout}>退出</button></>}
